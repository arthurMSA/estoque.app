'use client'

import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#113946',
    },
    secondary: {
      main: '#78A083',
    },
  },
})

export default theme